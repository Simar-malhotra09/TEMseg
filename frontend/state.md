[CLEAN] all commits referencing this file are meant to discuss overall state of the project
[STATE][CLIENT] we need to query backend on inital load to get all available models or better yet store it in a cache somewhere; when on a agiven session ie the same image, we are able to inference both models but the workspace doesn't get updated, works fine internally as shown by the console though
